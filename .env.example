DATABASE_URL="postgresql://postgres:postgres@localhost:5432/cargo_tracking_db"
JWT_SECRET="your_jwt_secret_change_this_in_production"
JWT_ACCESS_EXPIRES_IN="15m"
JWT_REFRESH_EXPIRES_IN="7d" //needs verification
PORT=5000
FRONTEND_URL="http://localhost:3000"
NODE_ENV="development"
# Optional Redis (falls back to in-memory if not set/unavailable)
REDIS_URL=""
